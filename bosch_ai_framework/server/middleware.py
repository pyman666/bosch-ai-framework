"""Server middleware — rate limiting, request ID, etc."""

from __future__ import annotations


# Stub — rate limiting middleware will be added here when needed.
# For now, services can use FastAPI's built-in middleware or bring their own.
